import Link from 'next/link';

export default function NewRestaurantSuccess() {
  return (
    <div>
      Restaurant created. Start adding items on the update restaurant page when it's made!
    </div>
  )
}